Homework guides:
- kconfig
- generate I/O reads and writes and test it
- glob -> shell pattern match
- shell specific regex: Glob

-----------------------------------
list_for_each (p,list)

|:

foo | bar | baz 
(foo -> bar) -> baz

Tee:
      _> bar
foo _|
     |_> baz

Tee: qemu -... | tee output.txt

tail -f output.txt

grep output.txt | python pyplot.py

- only eddit addrequest
------------------------------------
actual queues (Kfifo):
- <linux/kfifo.h>
- kfifo-alloc
- kfifo_in (Push)
- kfifo_out (Pop)
- kfifo_size (size in bytes of buffer)
- kfifo_len (number of data elements)
- kfifo_is_empty
- kfifo_is_full - according to kfifo_alloc

Associative container
- dictionary
- map
 - Kernel calls it an IDR
 - only use: maps uid -> pointer
- hash tables are an example of the associative map
- Binary trees:
 - BSTs
 - trie (rapid lookup systems , fast and used for internet)
 - RB-Trees
 - AVL
 - radix trees
 - radix sort (is a linear algorithm for sorting)
 - zoning improvment plan : ZIP
 - bitmaps

------------------------------------
Interrupts:
- it notifies the software that an event took place
- halts ongoing process on CPU
- Interrupt vectors: list of all interrupts
- PIC: processor interrupt controller (nearly no overhead)
- APIC: advanced PIC: allows share of IRQs
- top half is hardware critical 
 - for example clear an important buffer, network packages, etc
 - Interrupts cannot be blocked
 - no cuncurrency
- 

 _____________
|  TOP  HALF  |
|_____________|
|             |
|             |
|             |
| BOTTOM HALF |
|             |
|             |
|             |
|_____________|

